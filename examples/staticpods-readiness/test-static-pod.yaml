apiVersion: v1
kind: Pod
metadata:
  name: test-static-pod
  namespace: kube-system
  labels:
    app: test-static-pod
    component: test
spec:
  containers:
  - name: pause
    image: registry.k8s.io/pause:3.9
    resources:
      requests:
        cpu: 10m
        memory: 16Mi
      limits:
        cpu: 10m
        memory: 16Mi
